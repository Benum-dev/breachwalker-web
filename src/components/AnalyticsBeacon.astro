---
import { SITE } from "../data/site";

const token = SITE.cfWebAnalyticsToken;
---

{
  token ? (
    <script
      is:inline
      defer
      src="https://static.cloudflareinsights.com/beacon.min.js"
      data-cf-beacon={`{"token": "${token}"}`}
    />
  ) : null
}
